<div class='col-md-3'>
<h2>User info</h2>
<!-- モックのため、固定値や未実装になっている部分があります。アプリ完成前に修正しましょう。 -->
<table class='table'>
  <tr> 
    <%= image_tag 'default-image.jpg' %>  <!-- 固定値になっているので変数を使いましょう -->
  </tr>
  <tr>
    <th>name</th>
    <th><%= user.name %>
    </th>  
  </tr>
  <tr>
    <th>introduction</th>
    <th><%= user.introduction %>
    </th>  
  </tr>
</table>
<div class='row'>
  <%= link_to edit_user_path(user) , class:"btn btn-outline-secondary btn-block" do%>
    <i class="fa-solid fa-user-gear"></i>
  <% end %>
</div>
<h2 class="mt-3">New book</h2>
<%= form_with(model: book, url: books_path, local: true) do |form| %>
  <label for"book title">Title</label><br>
  <%= form.text_field :title %><br>
  <label for"book body">Opinion</label><br>
  <%= form.text_area :body %>
  <%= form.submit 'Create Book', class: "btn btn-sm btn-success" %>
<% end %>
</div>