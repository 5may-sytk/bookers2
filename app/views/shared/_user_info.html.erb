<div class='col-md-3'>
<h2>User info</h2>
<!-- モックのため、固定値や未実装になっている部分があります。アプリ完成前に修正しましょう。 -->
<table class='table'>
  <tr> 
    
    <%= image_tag 'default-image.jpg' %>  <!-- 固定値になっているので変数を使いましょう -->
  </tr>
  <tr>
    <th>name</th>
    <th><%= @user.name %></th>  <!-- 固定値になっているので変数を使いましょう -->
  </tr>
  <tr>
    <th>introduction</th>
    <th><%= @user.introduction %></th>  <!-- 固定値になっているので変数を使いましょう -->
  </tr>
</table>
<div class='row'>
  <a href="">
  <i class="fa-solid fa-house"></i> 
<% end %>
</div>
  <!-- 編集リンクが未実装 -->
</div>
<h2 class="mt-3">New book</h2>
<%= form_with model: @book do |f| %>
  <label for"book title">Title</label><br>
  <%= f.text_field :title %><br>
  <label for"book body">Opinion</label><br>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book', class: "btn btn-sm btn-success"%>
<% end %>
</div>